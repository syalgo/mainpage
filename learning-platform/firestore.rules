rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The current app reads/writes user permissions only through the
    // Firebase Admin SDK on the trusted Next.js server.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
